"""CLI utilities for the bot.

Subcommands:
- `python -m app.cli.init_db` — buat tabel di DB.
- `python -m app.cli.import_leads --csv data/leads.csv` — import CSV ke DB.
- `python -m app.cli.restagger_outreach [--dry-run]` — atur ulang jadwal PENDING.
"""
